# Base URL of the Spendeka API (used by the Expo app to call text-to-transaction, scan-bill, etc.)
# Local dev: use your machine's IP or localhost (e.g. http://localhost:4000 or http://192.168.1.x:4000)
# Production: use your deployed API URL
EXPO_PUBLIC_API_BASE_URL=http://localhost:4000
