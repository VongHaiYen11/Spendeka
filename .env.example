# Spendeka API – app calls this URL (text-to-transaction, scan-bill, image-caption).
# Default: deployed API on Render. Override only if you run API locally.
EXPO_PUBLIC_API_BASE_URL=https://spendeka-backend-2.onrender.com
